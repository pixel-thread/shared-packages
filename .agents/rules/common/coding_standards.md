# Coding Standards baseline

## Core Guidelines

- kebab-case for file names. PascalCase for components.
- React components must follow clean styling, dynamic color states, no ad-hoc borders (tonal hierarchy preferred).
- Use absolute imports with "@src/" alias.
- Prefer explicit React hooks for APIs (React Query).
